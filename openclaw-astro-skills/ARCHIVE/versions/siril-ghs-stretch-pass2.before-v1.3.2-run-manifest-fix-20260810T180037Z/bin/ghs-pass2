#!/usr/bin/env bash
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
exec /home/peter/.openclaw/workspace/agents/codewarrior/AstroProcessor/.venv/bin/python \
  "$SCRIPT_DIR/../scripts/ghs_pass2_orchestrator.py" "$@"
